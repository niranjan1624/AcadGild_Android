<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="app.ai.niranjan.myapplication.Session4Assignment1"
    android:background="@color/Black">

    <Button
        android:id="@+id/ascend"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/ascend"
        android:onClick="ascend"
        android:layout_alignParentLeft="true"
        android:layout_marginLeft="8dp"/>
    <Button
        android:id="@+id/descend"

        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/descend"
        android:onClick="descend"
        android:layout_alignParentRight="true"
        android:layout_marginRight="8dp" />
    <ListView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/list_view"
        android:layout_marginTop="8dp"
        android:layout_below="@id/ascend">

    </ListView>

</RelativeLayout>
